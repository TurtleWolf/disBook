import React from 'react';
// import PropTypes from 'prop-types';
const MyInput = ({ onClick }) => (
  <form action="">
    <input id="message" autocomplete="off" /><button>Send-dis</button>
  </form>
);

export default MyInput; 